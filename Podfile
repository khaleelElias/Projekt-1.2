# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'school project' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for school project
  pod 'Firebase/Auth'
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'Firebase/Database'

  target 'school projectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'school projectUITests' do
    # Pods for testing
  end

end
